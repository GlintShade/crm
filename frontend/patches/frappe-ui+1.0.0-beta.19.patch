diff --git a/node_modules/frappe-ui/src/components/DatePicker/CalendarPanel.vue b/node_modules/frappe-ui/src/components/DatePicker/CalendarPanel.vue
index 896d35b..3efff43 100644
--- a/node_modules/frappe-ui/src/components/DatePicker/CalendarPanel.vue
+++ b/node_modules/frappe-ui/src/components/DatePicker/CalendarPanel.vue
@@ -113,10 +113,16 @@
                 "
                 @keydown.down.prevent="shiftFocus(cell.date.add(7, 'day'), 1)"
                 @keydown.home.prevent="
-                  shiftFocus(cell.date.subtract(cell.date.day(), 'day'), -1)
+                  shiftFocus(
+                    cell.date.subtract((cell.date.day() + 6) % 7, 'day'),
+                    -1,
+                  )
                 "
                 @keydown.end.prevent="
-                  shiftFocus(cell.date.add(6 - cell.date.day(), 'day'), 1)
+                  shiftFocus(
+                    cell.date.add((7 - cell.date.day()) % 7, 'day'),
+                    1,
+                  )
                 "
                 @keydown.page-up.prevent="
                   shiftFocus(
@@ -435,7 +441,10 @@ function focusInitialCell() {
 
 defineExpose({ focusInitialCell })
 
-const WEEKDAYS = ['S', 'M', 'T', 'W', 'T', 'F', 'S']
+// VOLTEO PATCH (issue #134): Polish weekday abbreviations, week starting
+// Monday (owner decision 2026-09-16), matching the Monday-first grid built
+// by `generateWeeks()` in ./utils.ts.
+const WEEKDAYS = ['Pn', 'Wt', 'Śr', 'Cz', 'Pt', 'So', 'Nd']
 
 function ariaSelected(cell: CalendarPanelCell): 'true' | 'false' {
   return cell.isSelected || cell.isRangeStart || cell.isRangeEnd
diff --git a/node_modules/frappe-ui/src/components/DatePicker/DatePicker.vue b/node_modules/frappe-ui/src/components/DatePicker/DatePicker.vue
index 86bd7d4..cb48bf9 100644
--- a/node_modules/frappe-ui/src/components/DatePicker/DatePicker.vue
+++ b/node_modules/frappe-ui/src/components/DatePicker/DatePicker.vue
@@ -60,7 +60,7 @@
           :current-year="currentYear"
           :current-month="currentMonth"
           :weeks="weeks"
-          today-label="Today"
+          today-label="Dzisiaj"
           :min="props.min"
           :max="props.max"
           v-model:focused-date="focusedDate"
diff --git a/node_modules/frappe-ui/src/components/DatePicker/DateRangePicker.vue b/node_modules/frappe-ui/src/components/DatePicker/DateRangePicker.vue
index a33cf45..ff53dce 100644
--- a/node_modules/frappe-ui/src/components/DatePicker/DateRangePicker.vue
+++ b/node_modules/frappe-ui/src/components/DatePicker/DateRangePicker.vue
@@ -66,7 +66,7 @@
             :current-year="currentYear"
             :current-month="currentMonth"
             :weeks="weeks"
-            :today-label="isDualPaneActive ? '' : 'Today'"
+            :today-label="isDualPaneActive ? '' : 'Dzisiaj'"
             :hide-next="isDualPaneActive"
             :hide-out-of-month="isDualPaneActive"
             :center-header="isDualPaneActive"
diff --git a/node_modules/frappe-ui/src/components/DatePicker/DateTimePicker.vue b/node_modules/frappe-ui/src/components/DatePicker/DateTimePicker.vue
index 0ec591f..09c2486 100644
--- a/node_modules/frappe-ui/src/components/DatePicker/DateTimePicker.vue
+++ b/node_modules/frappe-ui/src/components/DatePicker/DateTimePicker.vue
@@ -62,7 +62,7 @@
             :current-year="currentYear"
             :current-month="currentMonth"
             :weeks="weeks"
-            today-label="Now"
+            today-label="Teraz"
             :min="resolvedMin"
             :max="resolvedMax"
             v-model:focused-date="focusedDate"
@@ -82,7 +82,7 @@
               :typeable="props.allowCustomTime"
               side="bottom"
               align="start"
-              placeholder="Select time"
+              placeholder="Wybierz godzinę"
               :min="computedMinTime"
               :max="computedMaxTime"
               @change="onTimeChange"
diff --git a/node_modules/frappe-ui/src/components/DatePicker/utils.ts b/node_modules/frappe-ui/src/components/DatePicker/utils.ts
index 7d1ae5d..3bef72f 100644
--- a/node_modules/frappe-ui/src/components/DatePicker/utils.ts
+++ b/node_modules/frappe-ui/src/components/DatePicker/utils.ts
@@ -3,19 +3,21 @@ import type { Dayjs } from 'dayjs/esm'
 import type { DatePickerDateObj as DateObj } from './types'
 
 // Constant list of month labels
+// VOLTEO PATCH (issue #134): Polish month abbreviations. Upstream has no
+// prop for this list, so it is patched directly.
 export const months: string[] = [
-  'Jan',
-  'Feb',
+  'Sty',
+  'Lut',
   'Mar',
-  'Apr',
-  'May',
-  'Jun',
-  'Jul',
-  'Aug',
-  'Sep',
-  'Oct',
-  'Nov',
-  'Dec',
+  'Kwi',
+  'Maj',
+  'Cze',
+  'Lip',
+  'Sie',
+  'Wrz',
+  'Paź',
+  'Lis',
+  'Gru',
 ]
 
 // Start of given month (0-indexed month)
@@ -32,7 +34,14 @@ export function generateWeeks(
   // Always emit 6 rows × 7 days so the calendar height stays constant — both
   // across months in a single pane (no jiggle when navigating) and between
   // panes in dual-pane mode (where one month may span 5 weeks and the other 6).
-  const start = monthStart(year, monthIndex).startOf('week')
+  // VOLTEO PATCH (issue #134): week starts on Monday (owner decision
+  // 2026-09-16), not the Sunday that dayjs' locale-independent
+  // startOf('week') always returns. first.day() is 0 (Sun) through 6
+  // (Sat); (day() + 6) % 7 is how many days to step back to reach that
+  // week's Monday. CalendarPanel.vue's WEEKDAYS header and its Home/End
+  // keyboard handlers are patched to match this same Monday-first order.
+  const first = monthStart(year, monthIndex)
+  const start = first.subtract((first.day() + 6) % 7, 'day')
   const days: DateObj[] = []
   let d: Dayjs = start
   const sel = dayjs(selected)
diff --git a/node_modules/frappe-ui/src/components/ListView/ListSelectBanner.vue b/node_modules/frappe-ui/src/components/ListView/ListSelectBanner.vue
index 846acc0..38b36b4 100644
--- a/node_modules/frappe-ui/src/components/ListView/ListSelectBanner.vue
+++ b/node_modules/frappe-ui/src/components/ListView/ListSelectBanner.vue
@@ -54,7 +54,7 @@
               variant="ghost"
               @click="list.toggleAllRows(true)"
             >
-              Select all
+              Zaznacz wszystko
             </Button>
             <Button
               icon="lucide-x"
diff --git a/node_modules/frappe-ui/src/components/Menu/MenuRenderContentAsChild.vue b/node_modules/frappe-ui/src/components/Menu/MenuRenderContentAsChild.vue
index aab0324..a9d96d9 100644
--- a/node_modules/frappe-ui/src/components/Menu/MenuRenderContentAsChild.vue
+++ b/node_modules/frappe-ui/src/components/Menu/MenuRenderContentAsChild.vue
@@ -1,19 +1,44 @@
-<script setup lang="ts">
-import { cloneVNode, useAttrs, type VNode } from 'vue'
+<script lang="ts">
+import { cloneVNode, defineComponent, type VNode } from 'vue'
 import { getFirstRenderableElement } from '../../utils/vnode'
 
-const props = defineProps<{
-  content?: VNode | VNode[]
-}>()
+// Hoisted to module scope on purpose: the `:is` binding below must always
+// resolve to the SAME component reference across re-renders. The previous
+// implementation created a fresh `{ render: renderRootNode }` object
+// literal inline in the template on every render; Vue's patch algorithm
+// treats a changed `:is` reference as a different component type and fully
+// unmounts the old instance before mounting a new one, instead of patching
+// the existing DOM node in place. Any attrs flowing through this wrapper
+// (for example `data-highlighted`/`tabindex`, which reka-ui's menu items
+// toggle on pointermove/focus/blur) therefore destroyed and recreated the
+// rendered element on every hover, which could race a real mouse click's
+// mousedown/mouseup pair and silently drop the click (observed in
+// Chromium; WebKit's click synthesis tolerated it).
+const RenderRootNode = defineComponent({
+  name: 'MenuRenderContentAsChildRoot',
+  props: {
+    content: {
+      type: [Object, Array] as unknown as () => VNode | VNode[] | undefined,
+      default: undefined,
+    },
+  },
+  setup(props, { attrs }) {
+    return () => {
+      const node = getFirstRenderableElement(props.content)
+      return node ? cloneVNode(node, attrs) : null
+    }
+  },
+})
+</script>
 
-const attrs = useAttrs()
+<script setup lang="ts">
+import type { VNode } from 'vue'
 
-function renderRootNode() {
-  const node = getFirstRenderableElement(props.content)
-  return node ? cloneVNode(node, attrs) : null
-}
+defineProps<{
+  content?: VNode | VNode[]
+}>()
 </script>
 
 <template>
-  <component :is="{ render: renderRootNode }" />
+  <RenderRootNode :content="content" />
 </template>
diff --git a/node_modules/frappe-ui/src/components/TextEditor/extensions/suggestion/createSuggestionExtension.ts b/node_modules/frappe-ui/src/components/TextEditor/extensions/suggestion/createSuggestionExtension.ts
index e7f8ff6..57ca30d 100644
--- a/node_modules/frappe-ui/src/components/TextEditor/extensions/suggestion/createSuggestionExtension.ts
+++ b/node_modules/frappe-ui/src/components/TextEditor/extensions/suggestion/createSuggestionExtension.ts
@@ -71,14 +71,26 @@ export function createSuggestionExtension<TItem extends BaseSuggestionItem>(
                   props: props,
                 })
 
-                if (!props.clientRect || !component.element) {
+                if (!props.clientRect) {
+                  return
+                }
+
+                // tiptap >=3.27 dispatches onStart with items: [] while the
+                // async fetch is pending; SuggestionList's root v-if renders
+                // a comment node then, so component.element (computed from
+                // the rendered component's first element child) is null.
+                // component.el is the renderer's wrapper div, which always
+                // exists, so the popup can be anchored even before items
+                // arrive.
+                const content = component.element ?? (component as any).el
+                if (!content) {
                   return
                 }
 
                 const defaultTippyOptions: Partial<TippyProps> = {
                   getReferenceClientRect: props.clientRect as () => DOMRect,
                   appendTo: () => document.body,
-                  content: component.element,
+                  content,
                   showOnCreate: true,
                   interactive: true,
                   trigger: 'manual',
@@ -89,6 +101,10 @@ export function createSuggestionExtension<TItem extends BaseSuggestionItem>(
                   ...defaultTippyOptions,
                   ...options.tippyOptions,
                 })
+
+                if (!props.items?.length) {
+                  popup[0]?.hide()
+                }
               },
 
               onUpdate(props: SuggestionProps<TItem>) {
@@ -102,6 +118,12 @@ export function createSuggestionExtension<TItem extends BaseSuggestionItem>(
                   popup[0].setProps({
                     getReferenceClientRect: props.clientRect as () => DOMRect,
                   })
+
+                  if (props.items?.length) {
+                    popup[0].show()
+                  } else {
+                    popup[0].hide()
+                  }
                 }
               },
 
diff --git a/node_modules/frappe-ui/src/components/TimePicker/TimePicker.vue b/node_modules/frappe-ui/src/components/TimePicker/TimePicker.vue
index 37f25e6..162995e 100644
--- a/node_modules/frappe-ui/src/components/TimePicker/TimePicker.vue
+++ b/node_modules/frappe-ui/src/components/TimePicker/TimePicker.vue
@@ -70,6 +70,7 @@
           style="transform-origin: var(--reka-popover-content-transform-origin)"
           role="listbox"
           :aria-activedescendant="activeDescendantId"
+          @pointerdown.capture="onPanelPointerDown"
         >
           <button
             v-for="(opt, idx) in displayedOptions"
@@ -288,6 +289,30 @@ function onInteractOutside(event: Event) {
     event.preventDefault()
   }
 }
+
+// VOLTEO PATCH (issue #99 follow-up, WebKit): WebKit does not give a
+// clicked <button> focus by default (Chromium/Firefox do), so a click on an
+// option below fires mousedown on the button but leaves e.relatedTarget
+// null on the trigger input's blur -- the panelRef.value?.contains(next)
+// check in onBlur below can never see the button as the new focus target,
+// so onBlur ran commitTyped + isOpen=false immediately, unmounting the
+// panel before mouseup, so the click on the option never fired at all
+// (confirmed with Playwright webkit: pointerdown/mousedown land on the
+// button, mouseup lands back on the trigger input, no click event ever
+// dispatched). suppressBlurClose is a second, focus-independent signal set
+// on pointerdown anywhere in the panel (capture phase, so it runs before
+// blur) and cleared on the next global pointerup (after the browser has
+// already dispatched click, if any) -- it keeps the panel mounted long
+// enough for the option's own click handler to run in every browser.
+const suppressBlurClose = ref(false)
+function onPanelPointerDown() {
+  suppressBlurClose.value = true
+  const clear = () => {
+    suppressBlurClose.value = false
+  }
+  document.addEventListener('pointerup', clear, { once: true, capture: true })
+}
+
 const uid = Math.random().toString(36).slice(2, 9)
 
 const isOpen = ref(false)
@@ -510,6 +535,10 @@ function onBlur(e: FocusEvent) {
   // Clicks inside the popover panel re-focus options; treat those as still-focused.
   const next = e.relatedTarget as Node | null
   if (next && panelRef.value?.contains(next)) return
+  // VOLTEO PATCH (issue #99 follow-up, WebKit): see suppressBlurClose above
+  // -- relatedTarget is unreliable in WebKit for a button click, so this is
+  // the fallback signal that a panel interaction is in progress.
+  if (suppressBlurClose.value) return
   commitTyped(displayValue.value)
   isOpen.value = false
 }
